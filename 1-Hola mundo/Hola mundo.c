// Lo PRIMERO es ponerle nombre al archivo - Extension .C

#include<stdio.h>	//Incluye la biblioteca de entrada y salida
/*	Funcion= algo que toma una entrada, la procesa y la devuelve como salida
	La funcion Main es obligatoria, es la primera que se ejecuta
	La palabra int representa el tipo de la funcion 	*/
	
int main()
{
	printf("Hola mundo!");
	return 0;
}

/* 	; para separar ordenes, al final de cada orden
	Execute:
		Compile: traduce el codigo C a codigo binario
		Run: ejecutar en codigo binario
		Compile & Run: ambas cosas	*/
